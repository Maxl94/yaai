---
---

# REST API Reference

The YAAI server exposes a RESTful API under `/api/v1`. Below is the full OpenAPI
specification, auto-generated from the FastAPI application.

When running the server locally, interactive docs are also available at [http://localhost:8000/docs](http://localhost:8000/docs).

!!swagger ../openapi.json!!
